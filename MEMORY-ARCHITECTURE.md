# HME 记忆架构 V2（多级分层 + 索引覆盖）

> 在 core(L0) + archive(L1) 之上，引入 **标签索引覆盖** 与 **RAG 检索层**。
> 本文档区分「借鉴业界」与「HME 自研」，标注依据。

## 0. 业界评估结论（先调研，后动手）

| 设计点 | 业界现状 | 结论 |
|---|---|---|
| 多级记忆 hot/warm/cold + 逐级沉降 | MenGPT(源头)、OpenClaw、surreal-memory 均成熟 | **借鉴机制**，不自造；但都用向量/DB，HME 用纯文本差异化 |
| 覆盖/去重/新盖旧 | Mem0 用 embedding；纯文本 + 关键词覆盖 = **空白** | **HME 自研（卖点）** |
| 索引头/文件列表 | 少数人做，未成标准 | 重新设计贴合纯文本 |
| RAG 层 / 大文档 | 大量现成 | 复用 harness grep，不建向量库 |
| 到期/遗忘 TTL | Amazon/agent-zero 已有 | 参照实现，配合 LRU |

## 1. 分层模型（门纲目科属种，先做三层）

| 层 | 名称 | 上限 | 注入/检索 | 内容 |
|---|---|---|---|---|
| L0 | core：USER + MEMORY | 有界(1597/2584) | 每会话注入 | 高频、永远在场 |
| L1 | archive | 有界(131072) | recall 检索 | 方法/教训/经验 |
| L2 | RAG 检索层 | 有界(索引) | recall 检索全部记忆 | 大文档、导入资料索引 |
| L3+ | 冷存储链（预留接口） | 无界 | 沉降 | 溢出/古老压缩摘要 |

**规则**（每层一致）：有界但不删——**溢出或过期 → 压缩成摘要 → 沉降下一层索引头 → 本层删除原内容**。值得的必记下（不丢）+ 有界（不臃肿）。

## 2. 标签索引覆盖（HME 自研核心）

### 判定
- 每条记忆写入时，由模型打 **1-2 个主题标签**（如 #migration / #build / #physics）。
- 新增记忆**先查本层索引**：
  - **命中相同标签** → **覆盖**（新内容替换旧的，删旧索引+旧内容，后续行号后移）。
  - **未命中** → **追加**（索引末尾加一条 + 内容排到下一段）。

### 存储形态（C 方案：目录 + 每主题一文件）

```
.dsh/hme/
  archive/
    INDEX.md            # 主题 → 文件 + 最后更新
    migration.md        # 每条事实 § 分隔
    physics-methods.md
    ...每个主题一文件...
```

- **优点**：真索引、按主题覆盖天然、可审计、单个主题文件小不至于全文件重写。
- **迁移**：现有单文件 archive.md → 拆成目录，一次性脚本，会话不中断（记忆独立于插件版本，harness session log 管连续性）。

### 覆盖缩行
覆盖时：删旧文件条目 → 行号无需显式维护（文件重新渲染，行号自然对齐）。彻底解法：主题分文件，覆盖 = 重写该主题小文件，行号自然正确。

## 3. 标签来源

- 写入时利用 memory/archive 工具 description 里已有的「4 条蒸馏判据」，**追加**：`同时给本条打 1-2 个主题标签（#xxx）`。
- 可选（Phase 2）：模型自动补标签。

## 4. 到期/遗忘（TTL）

- `add` 可选 `ttl`（或 `expires`）；存于 `archive.meta.json`。
- 到期 → 不删档案内容，而是**标记过期 → 优先被覆盖/沉降**。
- 与 LRU 结合：召回失败 + TTL 过期 → 沉降建议。

## 5. RAG 检索层（L2，复用 grep）

- L2 不是向量库，而是**把 archive/ 目录 + 导入文档纳入统一 recall 检索范围**：
  - `recall` 默认搜 `.dsh/hme/archive/` + 用户指定的导入目录。
  - 底层用现有 `tool-fs-search`（grep）、纯文本、零向量库依赖。
- 语义相似（真 embedding）**暂不做**，留作 future work。

## 6. 内存占用 / 有界兼容

| 层 | 有界 | 溢出去哪 |
|---|---|---|
| L0 core | 硬上限 | 沉降 → L1 archive（压缩摘要） |
| L1 archive | 硬上限 131072 | 沉降 → L2 RAG 索引 |
| L2 RAG | 索引有界 | 冷存储链（预留） |
| L3+ | 无界 | — |

「值得的必记下」靠：沉降不删 + 多级扩容；「有界」靠：每层硬上限 + 压缩。

## 7. 实现顺序

- **P2.1（已规划）**：标签。archive 目录化（单文件→多文件+INDEX.md）、写入打标签、新增命中覆盖、覆盖缩行。
- **P2.2**：TTL + 到期优先沉降。
- **P2.3**：L2 RAG（recall 扩到导入目录 + 统一索引）。
- **P2.4**：L3+ 冷存储接口（预留，不首版做）。

## 8. 参考

- MenGPT（Lettara）：core/archival/recall 三级。
- OpenClaw：hot/warm/cold + 访问频率升降级。
- Mem0：add() 语义去重（embedding）。
- agent-zero：遗忘→归档→检索。
- Amazon Bedrock：遗忘机制。

## 9. 增强特性架构约束（v0.5 起，设计决策）

版本线：**核心**（v0.1–v0.2）= memory + archive + recall，跨对话长期记忆的基础，不设关闭开关；
**增强**（v0.3+）= banner / status / UI 等体验与工具，用 config 开关、启动时静态生效。

约束（六条）：

1. **核心不设开关**。memory + archive + recall 共同构成「跨对话长期记忆」，始终注册。
2. **增强各自独立**：每项增强一个 config 开关，默认值在 README 声明，互不绑定。
3. **增强只依赖核心**：可读 MemoryStore（核心数据），**不调用另一增强的工具/服务**（扁平依赖，禁止增强间互相引用）。
4. **开关即注册**：开关是启动时静态 config，增强在 apply 时按开关决定是否 register；不在运行时动态增删工具。
5. **冲突 fail loud**（三阶段）：
   - 阶段一·事前：配置界面（UI/命令行交互）主动显示该开关的依赖矩阵、冲突、建议，保存前告知——**不只在 README 里等用户自己翻**。
   - 阶段二·事中：启动加载配置时若冲突/缺依赖，立即中止，终端打印大红字报错（含配置文件路径、行号、缺什么、修复建议）。
   - 阶段三·事后：提供 `dsh doctor`（或 `dsh check`）诊断命令，随时扫描配置、提前发现潜在问题并给修复建议，无需等到启动报错。
6. **依赖矩阵写进 README** 作补充参考，但主动告知必须发生在配置操作界面，不是只放 README。

执行顺序建议：先阶段一（配置时提示），再阶段二（启动报错），最后阶段三（诊断命令）。
